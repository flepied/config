#!/bin/sh

for f in `ls $HOME/config/.??*|grep  -v '~$'`; do
    base=`basename $f`
    echo "installing $f in $HOME/$base"
    rm -f $HOME/$base
    # copy it first to have the right permissions
    cp $f $HOME/$base
    sed -e "s%@HOME@%$HOME%g" < $f > $HOME/$base
done

# install ends here
